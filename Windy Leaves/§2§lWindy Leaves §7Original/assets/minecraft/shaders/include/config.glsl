#version 150

const float height = 0.1;    // I think the default is the best, but you can play around with it more
const float frequency = 8.0; // I think the default is the best, but you can play around with it more
const float speed = 3000.0;  // 500 - 5000 works best